[project]
name = "email-classify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.1",
    "httpx>=0.28.1",
    "rich>=14.3.3",
]

[project.scripts]
email-classify = "email_classify.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
